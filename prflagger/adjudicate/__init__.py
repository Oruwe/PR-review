"""The rethink: each observation read against the repository's own code, charter
and norms, by a model that must cite what it relies on.

`situate` gathers the context without any model; `adjudicate` asks; `citations`
checks every reference the answer makes against real artifacts before anything
is stored. An adjudication or suggestion whose citations do not resolve is
dropped, and the report says so — the model can annotate, demote or suggest,
but it can never add a finding or state an unchecked claim.
"""

from __future__ import annotations

__all__: list[str] = []
