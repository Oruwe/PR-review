"""Deterministic structure: diffs, symbols, call graph, blast radius.

Nothing in this package calls an LLM. `ast` and `git` are exact, free and measured in
milliseconds; a model doing the same job is slower, costlier and less accurate.
"""

from __future__ import annotations

__all__: list[str] = []
